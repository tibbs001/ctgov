<div class="container-fluid">
<script>
$(document).ready(function(){
    $('#sortTable').dataTable();
});
</script>

<div class='table-responsive'>
<table id="sortTable" class='table table-striped table-condensed table-hover' width='100%'>
  <thead>
    <tr>
      <th>title</th>
      <th>started</th>
      <th>lead sponsor</th>
      <th>title</th>
    </tr>
  </thead>

  <tbody>
    <% @studies.each do |study| %>
      <tr>
        <td><%= study.brief_title %></td>
        <td><%= study.start_date %></td>
        <td><%= study.lead_sponsor.try(:agency) %></td>
        <td class='duke-btn'><%= link_to 'Details',study_path(study), :class => "btn btn-default btn-mini" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>

